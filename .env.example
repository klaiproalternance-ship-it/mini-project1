# DevOps Monitoring Dashboard Environment Variables
# Copy this file to '.env' and fill in the values

# API Key for access control (e.g. dev-secret-key)
API_KEY=dev-secret-key

# API Base URL for the Streamlit dashboard to connect to the backend (used in Docker Compose)
API_BASE_URL=http://api:8000
